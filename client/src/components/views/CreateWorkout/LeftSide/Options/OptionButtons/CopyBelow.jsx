import React from 'react'
import DefaultButton from './DefaultButton'

const CopyBelow = ({ ul, icon, setOpenOptions }) => {
  return (
    <DefaultButton ul={ul} icon={icon} setOpenOptions={setOpenOptions} />
  )
}

export default CopyBelow